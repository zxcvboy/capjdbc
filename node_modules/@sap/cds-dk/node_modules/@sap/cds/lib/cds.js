// For compatibility w/ former uses
module.exports = require('.')

/* eslint-disable no-console */
if (process.env._warn_on_require_lib_cds) console.trace (`
  WARNING: Invalid import of '@sap/cds/lib/cds'
  Imports like require('@sap/cds/lib/cds') should never have been done,
  and will likely stop working in near future.
`)