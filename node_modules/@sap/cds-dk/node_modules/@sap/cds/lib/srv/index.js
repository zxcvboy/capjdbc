const { builtin, lazified, extend } = require ('@sap/cds-reflect/lib')
require = lazified(module)  // eslint-disable-line

/**
 * The `cds.builtin.classes.service` decorated with accessors to
 * service bindings, providers, runtime classes, etc.
 */
module.exports = exports = extend (builtin.classes.service) .with ({
    bindings: require ('./bindings'),
    factory: require('./factory'),
    providers:[],
    impl: x=>x,
})
