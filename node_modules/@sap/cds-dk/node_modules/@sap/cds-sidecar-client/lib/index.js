const authManager = require('./auth_manager');
const settingsManager = require('./settings_manager');

module.exports = {
    extend: require('./extend').run,
    apply: require('./apply').run,
    login: authManager.loginAndUpdateSettings,
    logout: authManager.logout,
    question: require('../lib/helper/question'),
    config: settingsManager.config
};
