"use strict";
/* Copyright (c) 2020 SAP SE or an SAP affiliate company. All rights reserved. */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getExpand = void 0;
var common_1 = require("../../common");
var one_to_many_link_1 = require("../../common/selectable/one-to-many-link");
var get_filter_1 = require("./get-filter");
var get_select_1 = require("./get-select");
function prependDollar(param) {
    return "$" + param;
}
function getExpand(expands, entityConstructor) {
    if (expands === void 0) { expands = []; }
    return expands.length
        ? {
            expand: expands
                .map(function (expand) { return getExpandAsString(expand, entityConstructor); })
                .join(',')
        }
        : {};
}
exports.getExpand = getExpand;
function getExpandAsString(expand, entityConstructor) {
    var _a;
    if (expand instanceof common_1.AllFields) {
        return '*';
    }
    var params = {};
    if (expand instanceof common_1.Link) {
        params = __assign(__assign(__assign({}, params), get_select_1.getSelect(expand._selects)), getExpand(expand._expand, expand._linkedEntity));
        if (expand instanceof one_to_many_link_1.OneToManyLink) {
            params = __assign(__assign(__assign(__assign({}, params), get_filter_1.getFilter(common_1.and.apply(void 0, (_a = expand._filters) === null || _a === void 0 ? void 0 : _a.filters), entityConstructor)), (expand._skip && { skip: expand._skip })), (expand._top && { top: expand._top }));
        }
        var subQuery = Object.entries(params)
            .map(function (_a) {
            var key = _a[0], value = _a[1];
            return prependDollar(key) + "=" + value;
        })
            .join(';');
        var subQueryWithBrackets = subQuery ? "(" + subQuery + ")" : '';
        return "" + expand._fieldName + subQueryWithBrackets;
    }
    return '';
}
//# sourceMappingURL=get-expand.js.map